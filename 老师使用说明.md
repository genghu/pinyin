# 拼音读读读 · 老师使用说明

面向**印尼零基础大学生**的汉语拼音学习网页（中文 + 印尼语双语）。
全部发音为**真人录音**（本地 mp3），**不需要联网**，手机、电脑浏览器直接打开 `index.html` 即可使用。

## 六大板块

| 板块 | 内容 | 建议用法 |
|---|---|---|
| 🔤 声母 | 23 个声母分 7 组点读，附中印尼双语发音要领 | 课堂上"连读本组"带读，学生跟读 |
| 🎵 韵母 | 24 个韵母分 4 组（单韵母 / 复韵母 / 前鼻 / 后鼻） | 同上；ü 是印尼学生难点，重点练 |
| 🎶 声调 | 四声卡片（妈麻马骂）+ 声调走向图 + 四声连唱 | 先听 mā má mǎ mà，再换音节连唱 |
| 🧩 拼读 | 声母 + 韵母 自由组合，自动判断能否相拼 | 让学生自己"拼"，理解拼音结构 |
| 📋 音节表 | 415 个音节 × 四声全部可点读，可整组连读、可搜索 | 课后自主刷读；可按声母组布置作业 |
| 🎙️ 跟读 | 听标准发音 → 录自己的音 → A/B 对比（录音只存在学生手机内存里） | 纠音课 / 自测 |

## 常用操作

- **🐢 慢速：关/开** —— 全局慢速播放（慢放不变调），跟读模仿时打开。
- **⏹ 停止** —— 随时停掉正在播的音或连读。
- **▶ 连读** —— 自动逐个播放并高亮，像课堂带读一样。

## 修改教学内容

- 发音要领、印尼语提示、分组、示例音节都在 **`js/data.js`**，用记事本即可编辑（保存为 UTF-8）。
- 页面样式在 `css/style.css`；不需要改 `js/app.js` 和 `js/audio.js`。

## 音频版权（必须保留）

`audio/` 内 1734 个 mp3 来自开源拼音录音包：
**Chen Wang 王琛 · Hugo Lopez · Nicolas Vion — CC-BY-SA-3.0 (2013)**
页面页脚已按要求署名，请勿删除；详见 `audio/yj/ATTRIBUTION.md`。

- 声母录音在 `audio/sm/`，韵母在 `audio/ym/`（ü 写作 yu：`yu.mp3`=ü、`yue.mp3`=üe、`yun.mp3`=ün；资源包缺 a.mp3，程序自动用 `yj/a1.mp3` 代替）
- 音节在 `audio/yj/`，文件名 = 拼音 + 声调数字，ü 写作 v（`lv3.mp3` = lǜ）；jǜ 特殊（`jv4.mp3`），程序已自动处理
- 目录和文件名**全部为英文数字**：压缩包在任何系统（Windows / Mac / Linux / 安卓）解压都不会乱码

## 兼容与部署

本网页为全浏览器兼容版，无需联网、无外部依赖（不用谷歌字体、不用 CDN）。
详见《[部署与兼容说明.md](部署与兼容说明.md)》。
