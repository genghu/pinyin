# 部署与兼容说明 · Panduan Deployment & Kompatibilitas

「拼音读读读」为**全浏览器兼容版**：不联网、不调用任何外部资源（不用谷歌字体、不用国外 CDN、不用框架），全部文件在本地，国内、国外访问速度一样快。

---

## 一、兼容范围 · Rentang Kompatibilitas

| 环境 | 支持情况 |
|---|---|
| Chrome / Edge / Firefox / Safari（近十年任意版本） | ✅ 完全支持 |
| 手机：iOS Safari、安卓 Chrome、华为/Xiaomi 自带浏览器 | ✅ 完全支持 |
| **微信、QQ 内置浏览器**（学生最常用） | ✅ 完全支持 |
| 360、QQ、UC、搜狗浏览器（**极速/高速模式**） | ✅ 完全支持 |
| 360 等浏览器**兼容模式**（老 IE 内核）、IE11 | ⚠️ 基本可用（点读、连读正常，个别动画效果简化） |
| Windows XP 老机房（Chrome 49 / 2016 年内核） | ✅ 可用 |

技术措施：JS 使用 ES5 语法（含 Promise 垫片）；CSS 不用 Grid 和 flex-gap、颜色均带老浏览器兜底值；音频目录/文件名全部为英文数字，压缩包跨系统解压不乱码；`X-UA-Compatible` 强制 IE 使用最新内核。

> 提示学生：360/QQ/UC 浏览器若显示异常，点地址栏的闪电/e 图标切换到「极速模式」即可。

---

## 二、三种使用方式 · Cara Pakai

### ① 本地打开（最简单，无需任何服务器）
把整个 `pinyin-dududu` 文件夹拷给学生（U盘 / QQ / 邮箱 / 网盘均可），
学生**双击 `index.html`** 即可使用，全部功能（含录音跟读）可用，无需联网。

### ② 局域网 / 学校服务器
把整个文件夹上传到学校网站服务器即可，例如放在 `https://学校域名/pinyin/`。
- 服务器需支持 UTF-8（网页已带 `<meta charset="UTF-8">`，一般无需设置）。
- 服务器需支持 mp3 直链播放（静态文件服务器均可：Nginx / Apache / IIS / 宝塔）。
- **录音跟读功能要求 https 网址**（浏览器安全规定）；只有 http 时不影响其他功能，页面会自动提示。

### ③ 免费托管（生成一个链接发给学生）

**国外学生（推荐）：**
- GitHub Pages：新建仓库 → 上传整个文件夹 → Settings → Pages → 选 main 分支。
- 或 Netlify / Vercel：把文件夹拖进网页即可，自动获得 https 网址。

**国内学生（推荐）：**
- Gitee Pages（码云，需实名认证）：方式同 GitHub Pages，国内访问快。
- 或直接用方式①（本地打开）——本网页本来就不需要网络，这是国内学生最稳的方式。

> ⚠️ 不要把 GitHub Pages 链接直接发给国内学生——GitHub 在国内时好时坏。国内发压缩包/网盘，国外发在线链接，两边都不卡。

---

## 三、微信/QQ 里打开注意 · Catatan WeChat/QQ

- 在线网址（https）可以直接在微信里打开使用 ✅
- 微信**无法解压 zip**：发给学生的压缩包，请让学生用 QQ、邮箱或网盘接收，下载后用系统文件管理器解压，再用浏览器打开 `index.html`（微信"文件"预览打开的网页没有声音）。

---

## 四、常见问题 · FAQ

| 问题 | 原因与解决 |
|---|---|
| 点了卡片没有声音 | 手机在静音模式；或浏览器开了"省流/无图模式"。换 Chrome/Edge 试试 |
| 录音按钮提示无法使用麦克风 | 网页不是 https 也不是本地文件（file://）。改用 https 网址，或下载到本地打开；并在浏览器弹窗中点"允许" |
| 页面是乱码 | 用浏览器"打开方式"选 Chrome/Edge；不要用记事本另存过的文件 |
| 老电脑 360 兼容模式显示异常 | 切换到极速模式（地址栏闪电图标） |
| 想放到学校服务器但空间不够 | 整站约 17MB（音频 16MB）；可以只部署网页、音频用学校已有副本替换 |

---

## 五、目录结构 · Struktur Folder

```
pinyin-dududu/
├── index.html            网页入口（双击打开）
├── css/style.css         样式（含老浏览器兼容写法）
├── js/data.js            教学内容（老师可改提示语）
├── js/audio.js           播放引擎（ES5 + Promise 垫片）
├── js/recorder.js        录音（兼容老式麦克风接口）
├── js/app.js             页面逻辑（ES5）
├── audio/sm/  (23)       声母真人录音
├── audio/ym/  (23)       韵母真人录音（ü 写作 yu/yue/yun）
└── audio/yj/  (1688)     音节真人录音（ü 写作 v，lv3 = lǜ）
```
